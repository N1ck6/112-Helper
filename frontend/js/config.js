

window.APP_CONFIG = {
  // адрес backend REST API; в Docker-контуре — имя сервиса из docker-compose,
  // например "http://backend:8000/api", при локальной разработке — localhost
  API_BASE_URL: "http://localhost:8000/api",
};
