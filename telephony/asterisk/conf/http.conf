; HTTP-сервер Asterisk нужен только для ARI (Asterisk REST Interface).
; В этапах 1-4 мы используем AGI, а не ARI (см. README этапа 3 —
; почему выбран AGI), поэтому HTTP пока выключен. Оставляем файл как
; явную точку расширения на будущее, а не выпиливаем совсем.
[general]
enabled=no
